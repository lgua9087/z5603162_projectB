# Assistant routing

Codex was used for this project. The active project conventions and verification
gates are recorded in `AGENTS.md`; this file is retained only so another
assistant knows where to find the canonical instructions. AI-use evidence is in
`ai/`.
