# Build and test dependencies. The deployed app reads precomputed artifacts and
# therefore does not install or import VADER.
vaderSentiment==3.3.2
pytest>=8,<10
ruff>=0.15,<1
python-docx>=1.2,<2
reportlab>=4,<5
pymupdf>=1.26,<2
pypdf>=6,<7
pdfplumber>=0.11,<1
